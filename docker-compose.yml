services:
  resume:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./cv.md:/app/cv.md:ro
      - ./resume.json:/app/resume.json:ro
    restart: unless-stopped
    environment:
      - PORT=3000
      - CHROMIUM_PATH=/usr/bin/chromium
